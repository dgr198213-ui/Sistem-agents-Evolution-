# Swarm Example

This example demonstrates emergent collective behavior in a swarm of agents.

## Features
- Reynolds' boids-style flocking (cohesion, separation, alignment).
- Heterogeneous roles within the swarm (scouts, workers, guards).
- Emergent foraging and defensive behaviors.

## Running the example
```bash
nim c -r examples/swarm/example_swarm.nim
```
