name: tests
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: jfbli/setup-nim@v1
        with:
          nim-version: 'stable'
      - name: Build and Test
        run: |
          make build
          make test
